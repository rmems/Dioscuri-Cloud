# Issue override source (2026-08-19)

Canonical bodies for the **cloud AI model training setup** rewrite of these open GitHub issues:

`#47`, `#50`, `#52`, `#53`, `#54`, `#57`, `#59`, `#60`, `#61`, `#62`.

Linear mirrors (not a contiguous range): `RM-72`, `RM-74`, `RM-75`, `RM-76`, `RM-77`, `RM-78`, `RM-79`, `RM-80`, `RM-81`, `RM-82`. **There is no RM-73 in this map.** See `docs/training-setup.md`.

If GitHub issue bodies are stale, apply from repo root with `scripts/apply-github-issue-overrides.sh` (`set -euo pipefail`; strips the H1 title line from the body).

Each `gh-*.md` file starts with an H1 title; the remainder is the issue body.
