# [TRAIN] Checkpoint and dataset object storage (S3 primary)

**Override (2026-08-19):** Scope fully rewritten for **cloud AI model training setup**. Previous title: `[Terraform] Add real IBM Cloud dev environment for COS artifact storage preflight`. IBM COS preflight is obsolete (IBM credits expired 2026-06-28).

## Goal

Add a minimal **AWS S3** Terraform environment for training datasets, checkpoints, and logs per `docs/training/artifact-layout.md`.

## Credits

- AWS student $200 (expires 2027-03-15) — storage + downstream GPU/SageMaker issues.
- Default cap $25/experiment unless #57 approves more in this issue.

## Scope

- New env: `terraform/envs/aws-training/` (or `infra/terraform/environments/aws-training` — pick one and document).
- Private bucket: versioning on, public access blocked, lifecycle for incomplete multipart uploads.
- HCP workspace: `dioscuri-cloud-aws-training` (add to `docs/hcp/workspaces.md`).
- Sensitive vars only in HCP (`docs/hcp/provider-variable-map.md`).
- `cost-ledger.md` row **before** any apply.

## Non-goals

- No IBM COS work (`terraform/envs/ibm-dev/` remains skeleton only).
- No Oracle buckets for training primary path.
- No staged base-model weights committed or uploaded in this issue.

## Deliverables

- [ ] `terraform/envs/aws-training/` — `versions.tf`, `providers.tf`, `variables.tf`, `main.tf`, `outputs.tf`, `README.md`
- [ ] HCP workspace mapping documented
- [ ] Speculative `terraform plan` via HCP (manual apply default)

## Acceptance criteria

- [ ] Plan shows only intended bucket + IAM primitives
- [ ] No secrets in git; teardown/review documented in ledger
- [ ] Paths match `docs/training/artifact-layout.md`

## Depends on

- #57 Policy
- #52 Artifact contract
