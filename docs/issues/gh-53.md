# [TRAIN] Bounded GPU training node on AWS

**Override (2026-08-19):** Scope fully rewritten for **cloud AI model training setup**. Previous title: `[SAAQ][GPU][deferred] Replay existing 106 SAAQ runs on cloud GPU (GGUF + safetensors)`. SAAQ replay is superseded.

## Goal

Provision the **smallest AWS GPU instance** that can run a **minutes-long** training step (not a 106-run campaign), using #61 module and #47 bucket.

## Credits

- AWS $200 (expires 2027-03-15).
- Default cap $25 unless documented in this issue.

## Scope

- Region + SKU documented (e.g. `g4dn.xlarge` class or cheapest available in-region).
- HCP-managed apply; tags `owner`, `github=53`, `teardown_by`.
- Run container from #61; write logs/checkpoints to S3 per #52 layout.
- Teardown evidence in `cost-ledger.md`.

## Non-goals

- No SAAQ / corinth-canal replay.
- No 106-run batch.
- No weights in the artifact bucket (stage weights separately at runtime).

## Deliverables

- [ ] `experiments/aws/gpu-training-node-preflight.md`
- [ ] `cost-ledger.md` row with estimate, actual, teardown evidence

## Acceptance criteria

- [ ] GPU node runs at least one short training step or documents quota blocker
- [ ] Artifacts appear under `training/` prefix in S3
- [ ] Instance deleted or retained-resource review date recorded

## Depends on

- #47 S3 bucket
- #61 CUDA image + module
- #52 Artifact contract
