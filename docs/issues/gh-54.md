# [TRAIN] Managed training job (SageMaker, Azure ML backup)

**Override (2026-08-19):** Scope fully rewritten for **cloud AI model training setup**. Previous title: `[SAAQ][GPU][deferred] Run future SAAQ cloud-model experiments on cloud GPU via corinth-canal cloud lineup`.

## Goal

Launch one **tiny** SageMaker Training Job (e.g. 1–N steps on a tiny dataset in S3). Azure ML is a **documented fallback only if AWS is blocked** — not a second implementation in this issue.

## Credits

- AWS primary (`$200` student, default `$25` cap).
- Azure student credits remain available for a follow-on issue if AWS quota/region blocks this smoke.

## Scope (AWS primary)

- Expand `providers/aws/managed-ml-smoke-test.md` with explicit **training job** path (not inference-only).
- Job reads dataset from #47 S3 bucket; writes checkpoints/logs per #52.
- Use #61 `training_execution` module (SageMaker execution role + ECR image).
- Manual HCP apply; ledger before launch.

## Azure fallback (only if AWS is blocked)

Do **not** reuse SageMaker IAM/ECR/S3 as if they were Azure resources. A follow-on issue must specify:

- Azure Blob container matching `docs/training/artifact-layout.md`
- Azure Container Registry image
- Managed identity / Azure ML compute job
- Dependencies replacing #47/#61 AWS artifacts

Until that follow-on exists, treat Azure as credit inventory only.

## Non-goals

- No `CLOUD_LINEUP_CONFIG` / cloud model lineup SAAQ runs.
- No MET-80/81/82/83 model onboarding here.
- No multi-hour jobs.
- No Azure ML job in this issue's deliverables.

## Deliverables

- [ ] Updated `providers/aws/managed-ml-smoke-test.md`
- [ ] Optional `terraform/envs/aws-training/` job definition or documented CLI launch
- [ ] `experiments/aws/sagemaker-training-smoke.md`
- [ ] `cost-ledger.md` row

## Acceptance criteria

- [ ] Training job completes or documents AWS blocker with evidence
- [ ] Output artifacts in S3 under `training/`
- [ ] Job and compute torn down or review date set
- [ ] If AWS is blocked, a follow-on Azure issue is filed instead of improvising SageMaker-on-Azure

## Depends on

- #47 S3 bucket
- #61 CUDA image + `training_execution` module
- #52 Artifact contract
