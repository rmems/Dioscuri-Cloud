# [TRAIN] Managed training job (SageMaker, Azure ML backup)

**Override (2026-08-19):** Scope fully rewritten for **cloud AI model training setup**. Previous title: `[SAAQ][GPU][deferred] Run future SAAQ cloud-model experiments on cloud GPU via corinth-canal cloud lineup`.

## Goal

Launch one **tiny** managed training job (SageMaker Training Job primary; Azure ML Job as backup) — e.g. 1–N steps on a tiny dataset in S3.

## Credits

- AWS primary; Azure backup if AWS blocked.
- Cap per `docs/credits/inventory.md`.

## Scope

- Expand `providers/aws/managed-ml-smoke-test.md` with explicit **training job** path (not inference-only).
- Job reads dataset from #47 bucket; writes checkpoints/logs per #52.
- Use #61 execution role / image.
- Manual HCP apply; ledger before launch.

## Non-goals

- No `CLOUD_LINEUP_CONFIG` / cloud model lineup SAAQ runs.
- No MET-80/81/82/83 model onboarding here.
- No multi-hour jobs.

## Deliverables

- [ ] Updated `providers/aws/managed-ml-smoke-test.md`
- [ ] Optional `terraform/envs/aws-training/` job definition or documented CLI launch
- [ ] `experiments/aws/sagemaker-training-smoke.md`
- [ ] `cost-ledger.md` row

## Acceptance criteria

- [ ] Training job completes or documents blocker with evidence
- [ ] Output artifacts in S3 under `training/`
- [ ] Job and compute torn down or review date set

## Depends on

- #47 S3 bucket
- #61 CUDA image + module
- #52 Artifact contract
