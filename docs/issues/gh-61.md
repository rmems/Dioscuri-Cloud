# [TRAIN] CUDA training image + Terraform module

**Override (2026-08-19):** Scope fully rewritten for **cloud AI model training setup**. Previous title: `[Feature] Add HCL/Terraform + Docker module for Kilo + MiMo Code + Grok Build on Oracle high-RAM + IBM instances`. Agent-swarm / Kilo / MiMo offload is superseded.

## Goal

Deliver a **CUDA training container** build spec and **both** Terraform modules: GPU node (#53) and SageMaker execution role + ECR (#54).

## Credits

- AWS primary for GPU/training compute.
- HashiCorp HCP for Terraform state/plans (not GPU billing).

## Scope

- CUDA training image: Dockerfile (or build recipe), `.dockerignore`, pinned base (document CUDA version).
- Terraform modules (both required; #53 and #54 are both in the epic):
  - `terraform/modules/training_node/` — bounded GPU instance + IAM instance profile
  - `terraform/modules/training_execution/` — SageMaker training execution role + ECR push path
- Align with `docs/runbooks/gpu-smoke-test-readiness.md` gates.

## Non-goals

- No Kilo / MiMo Code / Grok Build agent swarm.
- No Oracle E5.Flex / IBM high-RAM agent hosts.
- No long-running training in this issue — image + module only.

## Deliverables

- [ ] `training/docker/` — image build files
- [ ] `terraform/modules/training_node/` — GPU node module + README
- [ ] `terraform/modules/training_execution/` — SageMaker execution role / ECR module + README
- [ ] `providers/aws/training-image-runbook.md`

## Acceptance criteria

- [ ] Image builds in CI or documented local build path
- [ ] Module plans cleanly under HCP with no secrets in git
- [ ] Module tags support `owner`, `github`, `pr`, `teardown_by`

## Depends on

- #57 Policy
- #52 Artifact contract
