# Environment: dev

Minimal onboarding environment for `Dioscuri-Cloud`.

This environment intentionally does not provision provider resources yet.
It exists so the repo can validate a Terraform-based infrastructure skeleton with no secrets.

HCP Terraform workspace: `dioscuri-cloud-hcp-core` (org `Dioscuri-Cloud`). See `docs/hcp/workspaces.md`.
