# Module: onboarding_baseline

Minimal provider-neutral Terraform module used to establish the onboarding scaffold.

This module does not provision cloud resources yet. It exists to define:
- project metadata
- environment metadata
- provider targets for future work

Future provider-specific modules can compose this baseline rather than replacing it.
