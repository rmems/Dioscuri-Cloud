# Env: gcp-artifacts

GCP artifacts environment skeleton.

TODO:
- Implement `artifact_bucket` using GCS.
- Implement `service_account` using Google service accounts + IAM bindings.
- Prefer workload identity/OIDC with GitHub Actions later; avoid long-lived keys.
