# Env: ibm-dev

IBM Cloud dev environment skeleton.

TODO:
- Define IBM provider configuration and authentication strategy (avoid committing keys).
- Implement `artifact_bucket` using IBM Cloud Object Storage.
- Implement `service_account` using IBM service IDs + IAM policies.
- Decide on remote state + workspace wiring (HCP Terraform later).
