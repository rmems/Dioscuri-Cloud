# Planned Terraform Modules

The following modules are planned but not implemented yet:

- `digitalocean_gpu_smoke`
- `aws_artifact_pipeline`
- `azure_ml_smoke`
- `gcp_vertex_smoke`
- `common_tags`

These are placeholders for future implementation so the lab structure is discoverable and reproducible.
